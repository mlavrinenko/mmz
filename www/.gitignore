/public
/.tola/
.DS_Store
# Live `mmz` captures and derived facts, regenerated by `just docs::gen`
# (www/generate.sh). Build artifacts — never committed, so a stale capture
# cannot be reviewed into the tree.
/generated
